name: receitop_mobile_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  http: ^1.4.0
  flutter:
    sdk: flutter
    

  # Modern UI
  google_fonts: ^6.2.1      # ajustado para versão compatível
  flutter_svg: ^2.0.0
  animations: ^2.0.5
  flutter_staggered_grid_view: ^0.6.2

  # State management
  provider: ^6.0.5

  # Navigation
  go_router: ^7.0.0

  cupertino_icons: ^1.0.8

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/

  # Exemplo de fontes customizadas (opcional):
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
